# Reference docklight format files - proprietary/internal use only
ref_docklight_formats/

# Plan-signal server (separate git repo)
plan-signal/

# Node modules
node_modules/upload this into github with dev-pending notes, before that i want to disable "ref_docklight_formats" from github, it should not upload to github what to do? if its already uploaded i wnat to remove that

# Build outputs
dist/
target/
build/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Environment files
.env
.env.local
.env.*.local

# Compiled binaries
test_tcp

# Logs
*.log
npm-debug.log*
